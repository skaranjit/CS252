#include "stackType.h"
#include "queueType.h"
#include "token.h"

void infixTopostfix(void* t, void* s, void* o);
//This function will take valid token and infix expression. It will then convert to postfix expression using stack and queue of type token. 
//Pre Condition : Token should be provided. The stackType of token list will be referenced and as for the queuetype as well. 
//Post Condition:  queuetype list will be updated.



