For 12-10:

Change all min to max and replace code in unorderedarray.cpp for min:

int unorderedArrayListType::max(){
    int max = *list;
    if(length ==0 ){
        cout << "The list is empty.";
    }
    else{
        for (int i=0;i<length;i++){
            if (max<list[i])
                max = list[i];
        }
        return max;
    }
