//FUnction to calculate Percentage
#include "calculate.h"
//FUnction to calculate Percentage
float calcPercent(int score, int maximum){
        double per;
       	float sc=score;
	per = sc/maximum;
	return (per*100);
}
